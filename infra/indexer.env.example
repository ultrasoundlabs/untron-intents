# Minimal config for running the indexer against a local Anvil chain.
#
# Usage:
# - copy to `infra/indexer.env`
# - update `POOL_CONTRACT_ADDRESS` (and optionally forwarders) after deployment
# - then `docker compose -f infra/docker-compose.yml up -d db db_migrate indexer`

# Only run the pool stream (optional).
# INDEXER_STREAM=pool

# Pool instance (UntronIntents / UntronIntentsIndex event stream)
POOL_RPC_URLS=http://host.docker.internal:8545
POOL_CHAIN_ID=31337
POOL_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
POOL_DEPLOYMENT_BLOCK=0

# Optional tuning (sensible defaults exist; uncomment to override).
# POOL_CONFIRMATIONS=0
# POOL_POLL_INTERVAL_SECS=1
# POOL_CHUNK_BLOCKS=2000
# POOL_REORG_SCAN_DEPTH=256

# Forwarders stream (optional, default is no forwarders).
# FORWARDERS_CHAINS=[]
# FORWARDER_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
